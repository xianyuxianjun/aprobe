[
  {
    "content": "先看哪些 Operation 还没被覆盖，再读分页接口的契约。",
    "tool_calls": [
      { "id": "c1", "name": "list_operations", "arguments": {} },
      { "id": "c2", "name": "get_operation", "arguments": { "operation_id": "getPagination" } }
    ]
  },
  {
    "content": "结构断言看不出业务规则，补三条。",
    "tool_calls": [
      {
        "id": "c3",
        "name": "submit_case",
        "arguments": {
          "id": "edge-pagination-total-matches-items",
          "operation_id": "getPagination",
          "summary": "业务规则：total 必须等于 items 的条数",
          "request": { "method": "GET", "path": "/contracts/pagination" },
          "assertions": [
            { "kind": "status", "in": [200] },
            { "kind": "json_schema", "response": "200" },
            { "kind": "json_path", "path": "$.items", "length_equals_path": "$.total" }
          ]
        }
      },
      {
        "id": "c4",
        "name": "submit_case",
        "arguments": {
          "id": "edge-empty-total-matches-items",
          "operation_id": "getEmptyList",
          "summary": "空列表也必须满足 total == len(items)",
          "request": { "method": "GET", "path": "/contracts/empty" },
          "assertions": [
            { "kind": "status", "in": [200] },
            { "kind": "json_schema", "response": "200" },
            { "kind": "json_path", "path": "$.items", "length_equals_path": "$.total" }
          ]
        }
      },
      {
        "id": "c5",
        "name": "submit_case",
        "arguments": {
          "id": "edge-redirect-is-not-followed",
          "operation_id": "getRedirect",
          "summary": "契约允许 302；要断言的是重定向本身没被跟随",
          "request": { "method": "GET", "path": "/contracts/redirect" },
          "assertions": [
            { "kind": "status", "in": [302] },
            { "kind": "header", "name": "location", "contains": "/contracts/required" }
          ]
        }
      }
    ]
  },
  { "content": "补完了：分页与空列表的业务规则，以及重定向语义。" }
]
