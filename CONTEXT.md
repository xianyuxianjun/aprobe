# aprobe

aprobe 是一个面向 OpenAPI 的、有界自主的接口契约测试 Agent：它读规范、设计测试用例，由人审阅后交给确定性代码执行，再诊断失败归因，并留下可回放的权威轨迹。

## Language

**接口测试**:
验证接口的功能与契约正确性，即状态码、响应结构与类型、字段值、业务规则是否满足预期。
_Avoid_: 安全测试、渗透测试、漏洞扫描

**Operation**:
OpenAPI 中一个方法加路径的组合，例如 `GET /pets/{id}`。
_Avoid_: endpoint、路由、接口

**Test Case**:
一条可人工审阅的测试意图，描述目标 Operation、输入、前置条件与预期信号。
_Avoid_: 测试脚本、测试用例集

**Assertion**:
Test Case 中一条声明式判定条件，描述"观察结果满足什么才算通过"。它不含可执行代码，由确定性代码求值。
_Avoid_: 校验、检查点

**Test Run**:
Test Case 的一次实际执行，包含请求、响应、目标环境信息、耗时与结论。
_Avoid_: 执行记录、测试结果

**Verdict**:
一次 Test Run 的结论，取值为通过、失败或无法判定。
_Avoid_: 结论、结果、状态

**Credential**:
调用被测接口所需的鉴权值，由用户显式提供。
_Avoid_: 密钥、token

**被测目标**:
用户显式配置的测试环境地址，必须落在允许范围内。
_Avoid_: 环境、服务、目标系统

**评估基准**:
仓库自带的、版本化的被测目标与配套样例，用于计量指标。
_Avoid_: 测试环境、mock

**Agent 循环**:
受预算约束的多步决策过程，Agent 在其中查询上下文、选择动作、观察结果并决定继续或停止。
_Avoid_: 工作流、pipeline、编排

**Agent Step**:
Agent 循环中的一次迭代，包含一次模型请求及其产生的工具调用。
_Avoid_: 轮次、迭代

**Agent Budget**:
一次 Agent 循环允许消耗的步数、时长与模型额度上限。
_Avoid_: 限制、配额、超时

**Tool Call**:
Agent 对某个已注册工具的一次调用，其输入输出必须符合该工具声明的结构。
_Avoid_: 函数调用、API 调用

**Trace**:
一次运行中按顺序记录的事实，包含每一步的模型请求、工具调用、输入输出摘要、预算消耗与终止原因。它是回放与评估的权威来源。
_Avoid_: 日志、执行历史

**降级模式**:
不使用任何模型的运行方式，由确定性逻辑驱动同一条流程。
_Avoid_: mock 模式、离线模式

**模型建议**:
模型对用例设计、失败原因或修复方向给出的解释与提议。
_Avoid_: 模型输出、AI 结论

## Domain boundaries

- 规范声明的内容不等于接口的实际行为。
- 模型建议不等于测试结论；只有 Assertion 的确定性求值能判定通过或失败。
- Test Run 失败不等于接口存在缺陷。
- 未能验证不等于不存在问题。
- 目标未被允许不等于无法测试，它只是不在本次范围内。
- 编排层的中间状态不等于权威轨迹。
- 预算耗尽不等于测试完成。
- 本项目的「接口测试」不等于「接口安全测试」。
